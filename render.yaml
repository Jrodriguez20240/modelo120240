# render.yaml
version: 1
services:
  - type: web
    name: modelo120240
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python ejecutar.py
    envVars:
      - key: PYTHON_VERSION
        value: "3.9"
